{
	"productName": "PontuAll Test",
	"version": "0.3.2",
	"identifier": "com.tockanest.pontuall.test",
	"build": {
		"beforeDevCommand": "bun run sidecar:build:test && bun run dev",
		"beforeBuildCommand": "bun run sidecar:build:test && bun run build",
		"devUrl": "http://localhost:3000",
		"frontendDist": "../out"
	},
	"app": {
		"withGlobalTauri": false,
		"windows": [
			{
				"label": "splashscreen",
				"title": "PontuAll Test — Configuração",
				"width": 920,
				"height": 640,
				"minWidth": 880,
				"minHeight": 580,
				"resizable": true,
				"visible": true,
				"url": "/splashscreen",
				"center": true,
				"closable": false
			}
		],
		"security": {
			"csp": "default-src 'self'; img-src 'self' data: asset: http://asset.localhost; style-src 'self' 'unsafe-inline'; script-src 'self'"
		}
	},
	"bundle": {
		"active": true,
		"targets": "all",
		"icon": [
			"icons/clock.ico"
		],
		"externalBin": [
			"binaries/pontuall-auth-test"
		],
		"windows": {
			"signCommand": "pwsh -NoProfile -ExecutionPolicy Bypass -File ../scripts/sign.ps1 %1"
		}
	}
}